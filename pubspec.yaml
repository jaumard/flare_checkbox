name: flare_checkbox
description: Checkbox widget with two or three state animation with a Flare animation of your choice 
version: 2.0.0+1
homepage: https://github.com/jaumard/flare_checkbox.git

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.7.8"

dependencies:
  flare_flutter: ^3.0.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
